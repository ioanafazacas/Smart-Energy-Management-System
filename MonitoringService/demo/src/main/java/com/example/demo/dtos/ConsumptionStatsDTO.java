package com.example.demo.dtos;

import java.util.UUID;

public class ConsumptionStatsDTO {

    public ConsumptionStatsDTO(UUID deviceId, double totalConsumption, double avgConsumption, double maxConsumption, double minConsumption, long exceededCount, int size) {
    }
}
